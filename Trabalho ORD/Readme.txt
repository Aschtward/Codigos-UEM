---Para testar o algoritmo algumas coisas são necessárias.

-Um arquivo contendo registros de tamanhos variáveis contendo 4 campos, sendo esses campos separados por |.
-Executar via linha de comando seguindo as operações descritas no main.